---
import { Font } from "astro:assets";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import Formulas from "../components/Formulas.astro";

import type { Color } from "../config/colores";
import { colores } from "../config/colores";

interface Props {
  formulas?: boolean;
  latex?: boolean;
  title?: string;
}
const { formulas = true, latex = false, title = "OPF" } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <Font cssVariable="--font-new-cm" />
    <title>OPF</title>

    {
      /* Cambiar solo cuando se requiera (latex={true} en la etiqueta <Layout>) */
    }
    {
      latex && (
        <>
          <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css"
          />
          <script
            is:inline
            defer
            src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"
          />
          <script
            is:inline
            defer
            src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js"
            onload="renderMathInElement(document.body, {delimiters: [{left: '$$', right: '$$', display: true}, {left: '$', right: '$', display: false}]});"
          />
        </>
      )
    }
  </head>
  <body>
    <Nav />
    {formulas && <Formulas />}
    <main>
      <slot />
    </main>
    <Footer />
  </body><script>
    import { animate, hover, inView } from "motion";

    hover(".motion-hover", (element) => {
      animate(element, { scale: 1.5 });

      return () => animate(element, { scale: 1 });
    });

    // Hover sutil para tarjetas: escala leve + sombra
    hover(".motion-card-hover", (element) => {
      animate(
        element,
        { scale: 1.03, boxShadow: "0 12px 24px rgba(0, 0, 0, 0.15)" },
        { duration: 0.2, easing: "ease-out" },
      );

      return () =>
        animate(
          element,
          { scale: 1, boxShadow: "0 4px 10px rgba(0, 0, 0, 0.03)" },
          { duration: 0.2, easing: "ease-out" },
        );
    });

    inView(".motion-fadein", (element) => {
      animate(
        element,
        { opacity: [0, 1], y: [15, 0] },
        { duration: 0.5, easing: [0.22, 1, 0.36, 1] },
      );
    });
  </script>
  <style define:vars={{ ...colores }} is:global>
    body {
      background-color: var(--crema);
      color: var(--negro);
      font-family: var(--font-new-cm), Georgia, serif;
      margin: 0;
      display: flex;
      flex-direction: column;
      min-height: 100svh;
    }

    .motion-hover {
      display: inline-block;
    }

    .motion-card-hover {
      display: block;
      transform-origin: center;
      will-change: transform, box-shadow;
    }

    main {
      margin: 1em;
      flex: 1;
      position: relative;
      z-index: 1;
    }

    /* TODO!!!
     * Utiliza una clase .a en lugar de 'a' tag para evitar que 
     * este estilo se aplique en link donde no queremos.
     * Sin embargo esto debe de ser implementado de mejor forma.
     * */
    /* Estilo de links:
     * - azul de la paleta
     * - sin subrayado
     * */
    .a {
      color: var(--azul);
      text-decoration: none;
    }

    /* Subrayado cuando pasa el raton */
    .a:hover {
      text-decoration: underline;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    ul li {
      margin-bottom: 0.8rem;
      padding-left: 1.5rem;
      position: relative;
      line-height: 1.3;
    }

    ul li::before {
      content: "•";
      color: var(--dorado);
      font-size: 1.5rem;
      position: absolute;
      left: 0;
      top: -5px;
    }

    hr {
      border: none;
      border-top: 0.6px solid var(--gris);
      margin: 0.7em 0;
    }
  </style>
</html>
